# ── Server ────────────────────────────────────────────────
server:
  port: 8080

# ── Database ──────────────────────────────────────────────
# Override with env vars in production: DB_URL, DB_USERNAME, DB_PASSWORD
spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:195.35.61.109}:${DB_PORT:3306}/${DB_NAME:u329011707_smagyo}?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: ${DB_USERNAME:u329011707_smagyo_dev}
    password: ${DB_PASSWORD:fyMC:8hD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  # ── JPA / Hibernate ───────────────────────────────────────
  # use "validate" in production after first run
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        dialect: org.hibernate.dialect.MySQLDialect

# ── JWT ──────────────────────────────────────────────────
# Secret must be ≥ 256-bit (32 chars). Set JWT_SECRET env var in production.
jwt:
  secret: ${JWT_SECRET:SmagyoSecretKey2026SmagyoSecretKey2026SmagyoKey!!}
  # 24 hours in milliseconds
  expiration-ms: ${JWT_EXPIRATION:86400000}

# ── CORS ─────────────────────────────────────────────────
# React dev server. Add production domain with comma separation.
cors:
  allowed-origins: ${CORS_ORIGINS:http://localhost:5173}

# ── Logging ───────────────────────────────────────────────
logging:
  level:
    com.smagyo: INFO
    org.springframework.security: INFO

